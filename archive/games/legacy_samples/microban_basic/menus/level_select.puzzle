scene level_select {
	view {
		message = "Select a level"
		text message
		level_menu {
			show_index = false
			show_solved = true
			button "Title" -> input back_to_title
		}
	}
	on_scene_start {
		stop_music music_name
	}
	rules {
		if input == back_to_title -> goto title
	}
}
