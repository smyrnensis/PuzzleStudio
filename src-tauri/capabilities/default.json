{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the PuzzleStudio editor window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
