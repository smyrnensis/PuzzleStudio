# Design Principles

この文書は、このプロジェクトの最上位の設計原則を定義する。

これは実装仕様書ではない。ルール言語のリファレンスでも、エディタの設計書でもない。
この文書の役割は、後続の設計判断を制約することである。何を守るべきか、何を避けるべきか、なぜこのプロジェクトが汎用ゲームエンジンではなく特殊化された環境として存在するのかを定義する。

## 目的

このプロジェクトは、ターンベース・グリッドベース・ルール駆動型パズルゲームを制作するための軽量な環境である。

中心的な目的は、人間の意図、AI によるルール化、決定論的な実行、具体的な検証のあいだに、短い反復フィードバックループを作ることである。

人間は、作りたいパズル的因果、望ましいプレイ体験、何が面白いかの最終判断を持つ。
AI は、その意図をルール、条件、例、テスト、レベル案へ変換する。
エンジンは、それらを決定論的に実行し、結果、差分、トレース、反例を検査可能な形で返す。

このシステムは、次の往復を短くするために存在する。

- 意図
- ルール表現
- 実行
- 観察
- 修正

目的は、あらゆるゲームを作れることではない。この形式のパズルゲームを、異常に明瞭で、検査可能で、反復しやすいものにすることである。

## 中心思想

パズルゲームの面白さは、プレイヤーが状態を読み、操作を選び、その結果を理解できることにある。

そのため、ゲームの振る舞いはできるだけ明示的なルールとして表現されるべきである。ルールは人間が読めるだけでなく、AI が編集でき、エンジンが検証できる形で管理される。

制作環境の目的は、高機能なビジュアルエディタを作ることではない。
目的は、意図、ルール、実行結果、反例の間に短いフィードバックループを作ることである。

## 哲学

### 可視性

ゲーム内の重要な条件は、人間が観測または検査できる形で存在するべきである。

プレイヤーの判断、パズルの可解性、ルールの挙動に影響する条件は、盤面、明示的な表示、トレース、またはデバッグ説明から読み取れることが望ましい。

隠れた状態は絶対に禁止ではない。しかし、慎重に扱う必要がある。隠れたフラグ、暗黙のタイマー、観測不能な確率、説明不能な副作用は、プレイヤーがパズルを推論する力を弱める。

可視性とは、単に画面に表示されていることではない。
人間が「なぜそうなったのか」と問うたときに、明示的な状態と明示的なルールに基づく答えを得られることを意味する。

### 操作性

プレイヤーと制作者は、ゲーム状態を制御し、推論できるべきである。

#### 空間的操作性

空間はグリッドベースで扱う。

グリッドは、位置、隣接、衝突、包含、到達可能性、変換を離散的に表現する。これにより、空間的関係は読みやすく、制御しやすくなる。

連続座標はコアモデルの外に置く。連続配置は、多くの場合、この種のパズルロジックにとって不要または有害な曖昧さを持ち込む。

#### 時間的操作性

時間はターンベースで扱う。

プレイヤーは状態を読み、操作を選び、結果を観察し、計画を修正する。このシステムは、その思考のリズムを直接支えるべきである。

undo と restart は補助機能ではない。試行錯誤と思考の一部である。
特定のゲームが明示的なルールとして制限しない限り、システムは無制限の undo と restart を前提にする。

### 普遍性

ゲームの中心的な面白さは、抽象化に耐えるべきである。

すべてのオブジェクトを丸、三角、四角、色、文字、アイコンのような抽象記号に置き換えても面白さの大部分が残るなら、そのゲームは強い構造を持っている。

視覚や音は重要である。ただし、それらは主に読みやすさ、手触り、アクセシビリティ、雰囲気を高めるためのものとして扱う。
意味の中心は、記号化しても残る因果構造に置く。

## 人間・AI・エンジンの役割

このシステムでは、三つの役割を分ける。

人間は、意図と判断を持つ。

- どのようなパズル的相互作用を作りたいか
- 何が面白い、意外、公平、洗練されていると感じるか
- 生成された挙動を受け入れるか
- どのエッジケースが重要か
- ルール体系が不透明になりすぎていないか

AI は、変換と探索を担当する。

- 意図をルール候補へ変換する
- ルール、フェーズ、条件、例を整理する
- テストケースや反例を生成する
- レベルやバリエーションを提案する
- トレースを説明し、失敗をデバッグする
- ルール変更を、その動機になった意図へ結びつける

エンジンは、実行と検証を担当する。

- ルールを決定論的に適用する
- 再現性を保つ
- 状態差分とトレースを出力する
- 例と回帰テストを実行する
- 意図と挙動の矛盾を露出させる

AI が生成したルールは、動いたというだけでは採用されない。
人間の意図との対応を保ち、例、トレース、プレイを通じた検証に耐えたときに採用される。

## 表現レイヤー

このシステムは、ユーザーコミュニケーション、記述文法、正規化されたルールモデル、実行状態、トレースを分けて扱うべきである。

- ユーザーコミュニケーション: 自然言語の意図、質問、例示、反例、プレイ感のフィードバック
- 記述文法: 人間と AI が読み書きしやすい表面表現
- ルールモデル: 安定した ID と明示的な関係を持つ正規化表現
- 実行状態: エンジンが決定論的に操作する状態
- トレース: 何が、なぜ起きたかの説明

これらのレイヤーは相互に生成されてもよい。しかし、混同してはならない。

たとえば、人間向けの記述文法は読みやすさを優先してよい。一方で、エンジンが扱うルールモデルは曖昧さを持たず、AI が差分編集しやすい構造であるべきである。

### 編集可能面は public contract として外に出す

エディタ、プレビュー、ランタイムの関係では、「内部を隠す」だけでは境界にならない。
エディタが必要とする操作面が明示されていなければ、エディタは結局 runtime fixture、scene JSON、renderer state などの内部表現を読みに行き、内部変更に追従する壊れやすい依存を作る。

したがって、エディタが正当に変更したいものは public contract として外に出す。
たとえば preview では、表示対象、カメラ、グリッドなどの編集可能な設定は runtime が明示的な更新 API として受け取り、エディタは runtime fixture 全体や scene/component の内部 schema を再構成しない。

これは runtime を editor から完全に独立させるという意味ではない。
むしろ逆で、editor が触るべき面を runtime 側の責任で定義することで、runtime は内部表現を自由に変えられ、editor は公開された control surface だけに依存できる。

この原則に反する兆候は次の通りである。

- エディタが preview runtime から内部 fixture を抜き出し、編集 state として再解釈している。
- エディタが scene/component の renderer 向け JSON を書き換えて、preview の見た目を操作している。
- runtime の内部 schema を変えると、編集対象の level や preview 設定まで壊れる。
- 「内部だから直接触らないはず」という期待だけで、実際に使える public API がない。

この場合の修正は、内部表現をさらに隠すことではなく、編集可能な面を小さく、名前付きで、テスト可能な contract として公開することである。

## ドキュメントの読者分離

ドキュメントは、ユーザー向けと開発者向けを分けて書くべきである。

ユーザー向けドキュメントは、作者が `.puzzle` を読み書きし、期待どおりに試し、失敗を直せることを目的にする。
ここでは、canonical な書き方、短い例、よくある制作手順、画面上または実行時に何が起きるかを優先する。
内部 IR、互換用の旧構文、実装上の都合、未確定の設計論は、ユーザーの最初の理解に混ぜない。

開発者向けドキュメントは、実装者が文法、lowering、runtime、adapter の境界を壊さずに変更できることを目的にする。
ここでは、なぜその構文が存在するのか、どの層が所有するのか、どの旧構文を canonical にしないのか、どの sibling case に同じ判断を適用すべきかを書く。

文法や挙動を追加したときは、二つの問いを分けて確認する。

- ユーザー向けには、この機能をどの mental model と例で説明するか。
- 開発者向けには、この機能がどの既存原則、所有者境界、構文パターンのインスタンスなのか。

ユーザー向け説明で例だけが増え、開発者向け説明で再利用される原則が増えないなら、その機能は局所的な特例になっている可能性が高い。

## 文法の一貫性と合成性

`.puzzle` の文法は、ユーザーが一度覚えた形を別の場所でも使えると感じられるように設計する。

同じ概念は、可能な限り同じ形で現れるべきである。
たとえば、条件は `if`、反復は `for` や `repeat`、所有者ごとの入力割り当ては `inputs { ... }`、lifecycle は `on_*`、scene command は command として直接書く、というように、構文の形が概念の形に対応するべきである。

これは単なる表記の統一ではない。
ある構文を覚えたユーザーが、「ここでも使えそうだ」と推測したとき、その推測が概念的に正しければ、文法もそれを受け入れる方向に寄せる。
逆に、同じ見た目なのに別の意味を持つ構文、同じ意味なのに場所ごとに別の書き方を要求する構文は、学習した直感を壊す。

ただし、すべての場所で同じ構文を機械的に許可することが一貫性ではない。
一貫性は、所有者と意味の一貫性である。
scene の lifecycle と puzzle の lifecycle が違うなら、同じ `on_*` 系でも置ける場所は違ってよい。
display routine と gameplay routine が違うなら、同じ routine 風の形を持っていても、読み書きできる state は違ってよい。

重要なのは、ユーザーの直感が外れたときに「なぜここでは違うのか」を所有者境界や実行モデルから説明できることである。

### 見た目から実行意味を推定しない

実装は、表示結果の合成、隣接、重なり、命名、または現在のサンプルでの使われ方から、作者が書いていない連動関係を推定してはならない。

ある object が別の object の見た目を補助しているように見えても、それだけで同じ移動、同じ衝突、同じアニメーション、同じ入力反応、同じ lifecycle を共有するとは扱わない。連動が必要なら、作者が `.puzzle` のルール、display routine、component contract、または明示的な設定として書くべきである。

これは不便を残すためではなく、観測された見た目と実行意味を混同しないためである。見た目から暗黙の companion、owner、group、fallback を作ると、別のゲームで同じ構文が現れたときに、作者の意図しない副作用になる。便利な推定を追加したくなった場合は、まずそれを表す明示的な authoring surface が必要かを検討する。

### 特例化と hardcode の点検

新しい機能を追加するときは、それが hardcode された一回限りの分岐ではないかを必ず点検する。

ある機能が本当に必要なら、多くの場合、それは既存の構文パターン、所有者境界、lowering 規則、component contract、または lifecycle の一例として説明できる。
説明できない場合、その機能は便利でも、文法全体の一貫性を弱める可能性がある。

hardcode の兆候は次のような形で現れる。

- 特定の名前、特定の component、特定の adapter だけが、文法上の一般概念を迂回している。
- 既存の `if`、`for`、`inputs`、routine、effect、scene command で表せるはずのものに、別の小さな専用言語を足している。
- 似た機能を追加すると、同じような分岐をもう一つ書く必要がある。
- ユーザー向けには自然に見えるが、開発者向けにはどの層が所有しているか説明できない。
- 互換性のための処理が canonical syntax の mental model を変えている。

文法の DRY は、単に parser code の重複を減らすことではない。
同じ意味の構造を、同じ概念、同じ所有者、同じ lowering 経路として扱うことである。

この点で、文法の一貫性と hardcode 回避は同じ問題の二面である。
前者はユーザーの学習した直感を再利用するための原則であり、後者は実装がその直感を局所的な特例で裏切らないための原則である。

## コミュニケーションとしての例

例は、ユーザーとのコミュニケーションにおいて意図を固定するための道具である。

ユーザーが自然言語で説明した意図は、多くの場合まだ曖昧である。AI はその曖昧さを勝手に埋めることができてしまう。

そこで、最小盤面、入力、期待結果、反例を使って、意図の境界を確認する。

例は、後にテストへ変換されてもよい。しかし、最初の役割はテストではなく、ユーザーと AI の間で「何を意味しているか」を揃えることである。

意図が曖昧なとき、システムは抽象的な機構を増やすより先に、例を求めるか、例の候補を生成するべきである。

## ルールシステムの方向性

ルールは、離散的なゲーム状態に対する局所的で検査可能な変化を表現するべきである。

既存のルール駆動型パズル制作ツールから文法や考え方を借りることはできる。しかし重要なのは表面互換ではない。
重要なのは、盤面上の変化を、明示的なルールとして記述し、検査し、実行し、修正できることである。

ルールシステムは、次を重視する。

- 名前付きルール
- 名前付きオブジェクトとプロパティ
- 明示的なフェーズ
- 明示的な条件
- テストとしての例
- トレース可能な実行
- AI 編集のための安定した識別子
- 決定論的な競合解決

ルールシステムは、次を避ける。

- ルール内の任意コード
- 暗黙の副作用
- 無制限に肥大化するグローバル状態
- 見えない依存関係
- トレースで説明できない挙動
- パズル推論を改善せず、一般性だけを増やす機能

追加の表現力は、局所ルールの核を不透明にするのではなく、その外側に足すべきである。

適切な拡張点の例:

- 名前付き query
- 名前付き condition
- macro
- phase
- event
- validation rule
- trace annotation
- intent link

## ゲーム固有性の優先

このシステムは、作りたいゲーム群の性質に合わせて偏ってよい。

ある機能がすべてのゲームにとって一般的に有用である必要はない。
この形式のルール駆動型パズルゲームにおいて、明瞭さ、表現力、反復速度、検証可能性を高めるなら、その機能には価値がある。

汎用性は自動的に価値ではない。
より狭い機能によって、意図したゲームが読みやすく、テストしやすく、発展させやすくなるなら、狭い機能を優先する。

## 制作環境の方向性

制作環境は軽量であるべきである。

大きなビジュアルエディタは主目的ではない。
まず必要なのは、速い反復を支える最小のワークフローである。

- 意図、ルール、例、レベルを編集する
- ゲームを実行する
- 何が起きたかを検査する
- AI の支援で修正する
- 回帰テストを残す

最も重要な制作面は UI ではない。
人間が読める意図、AI が編集できるルール構造、決定論的な実行、検証可能な出力の間にあるフィードバックループである。

### Web 版と Desktop 版のファイル境界

Web 版と Desktop 版は、同じ編集・プレビュー・コンパイルの中核を共有するべきである。
分岐してよいのは、ホスト環境に依存するファイルアクセスや権限の境界だけである。

Web 版は、ブラウザの制約に合わせて、フォルダ import、ブラウザ内編集、download/export を基本ワークフローにする。
Web 版はユーザーのローカルフォルダを継続的な編集対象として直接所有しない。

Desktop 版は、ユーザーが明示的に開いた project root だけを編集対象にする。
起動時にサンプルフォルダ、リポジトリ全体、ユーザーディレクトリなどを自動で読み込んではならない。
初期状態は空でよく、native open folder / open project によって初めてファイルシステム上のプロジェクトを持つ。

Desktop 版で直接ファイルを編集する場合も、JavaScript に広いファイルシステム権限を渡すことを前提にしない。
UI は「開く」「保存する」という意図をホストへ伝え、Rust 側のホストアダプタが project root 境界内で読み書きを行う。

Web 版と Desktop 版の仕様差は、最初から中核ロジックへ持ち込まない。
まず共有できる editor service / backend API を揃え、最後にホストアダプタで file access の差だけを分岐させる。

## 設計境界

このプロジェクトは意図的に特殊化されている。

プラットフォーマー、アクションゲーム、物理シミュレーション、リアルタイムゲーム、ノベルエンジン、任意のインタラクティブメディアを作るための汎用エンジンになるべきではない。

この境界は、デフォルトで乗り越えるべき制限ではない。
システムの明瞭さを守るための制約である。

新しい機能を追加するか判断するときは、それが可視性、操作性、普遍性、人間・AI・エンジンのフィードバックループを改善するかで評価する。

表現力は増えるが、ルールの挙動が見えにくく、テストしにくく、説明しにくくなる機能は、拒否するか、コアモデルの外に置くべきである。
