# Design Principles

この文書は、このプロジェクトの最上位の設計原則を定義する。

これは実装仕様書でも、`.puzzle` の文法リファレンスでもない。個別機能の
canonical syntax は `AUTHORING_SYNTAX.md`、現在の実行意味は
`CURRENT_SPEC.md`、実装上の所有者と作業手順は対象フォルダの `AGENTS.md`
が所有する。この文書には、それらの判断を長期的に制約する原則だけを置く。

## 目的

このプロジェクトは、ターンベース・グリッドベース・ルール駆動型パズルゲームを
制作するための軽量な環境である。

中心的な目的は、人間の意図、AI によるルール化、決定論的な実行、具体的な検証の
あいだに短い反復フィードバックループを作ることである。

```txt
意図 -> ルール表現 -> 実行 -> 観察 -> 修正
```

目的は、あらゆるゲームを作れることではない。この形式のパズルゲームを、異常に
明瞭で、検査可能で、反復しやすいものにすることである。

## 中心思想

パズルゲームの面白さは、プレイヤーが状態を読み、操作を選び、その結果を理解できる
ことにある。そのため、ゲームの振る舞いは、人間が読め、AI が編集でき、エンジンが
検証できる明示的なルールとして表現されるべきである。

制作環境の目的は、高機能なビジュアルエディタそのものではない。意図、ルール、
実行結果、反例の間に短いフィードバックループを作ることである。

## 判断軸

### 可視性

プレイヤーの判断、パズルの可解性、ルールの挙動に影響する条件は、盤面、明示的な
表示、トレース、またはデバッグ説明から検査できるべきである。

隠れた状態は絶対に禁止ではない。しかし、観測不能なフラグ、暗黙のタイマー、
説明不能な副作用によって「なぜそうなったのか」が答えられなくなる設計は避ける。

### 操作性

プレイヤーと制作者は、ゲーム状態を制御し、推論できるべきである。

- 空間は、位置、隣接、衝突、包含、到達可能性、変換を離散的に扱えるグリッドを
  コアモデルとする。
- 時間は、状態を読み、操作を選び、結果を観察できるターンを単位とする。
- undo と restart は補助機能ではなく、試行錯誤と思考の一部として扱う。ゲームが
  明示的に制限しない限り、システムは無制限の undo と restart を前提にする。

連続座標やリアルタイム進行が必要な表現は、パズルの決定論的な核と混同せず、その
外側に置く。

### 普遍性

ゲームの中心的な面白さは、抽象化に耐えるべきである。オブジェクトを丸、色、文字、
アイコンのような抽象記号へ置き換えても面白さの大部分が残るなら、そのゲームは強い
因果構造を持っている。

視覚や音は、読みやすさ、手触り、アクセシビリティ、雰囲気を高める。ただし意味の
中心は、記号化しても残る因果構造に置く。

## 人間・AI・エンジンの役割

人間は、意図と最終判断を持つ。

- どのようなパズル的相互作用を作りたいか
- 何が面白い、意外、公平、洗練されていると感じるか
- 生成された挙動を受け入れるか
- どのエッジケースが重要か

AI は、変換と探索を担当する。

- 意図をルール、条件、例、テスト、レベル案へ変換する
- 反例を生成し、トレースを説明する
- 変更を、その動機になった意図へ結びつける

エンジンは、実行と検証を担当する。

- ルールを決定論的に適用する
- 再現性を保ち、状態差分とトレースを出力する
- 例と回帰テストを実行し、意図と挙動の矛盾を露出させる

AI が生成したルールは、動いたというだけでは採用されない。人間の意図との対応を
保ち、例、トレース、プレイを通じた検証に耐えたときに採用される。

## 表現と所有者

ユーザーコミュニケーション、記述文法、正規化されたルールモデル、実行状態、
トレースを分けて扱う。

- ユーザーコミュニケーション: 自然言語の意図、質問、例示、反例、プレイ感
- 記述文法: 人間と AI が読み書きする表面表現
- ルールモデル: 安定した ID と明示的な関係を持つ正規化表現
- 実行状態: エンジンが決定論的に操作する状態
- トレース: 何が、なぜ起きたかの説明

これらは相互に生成されてもよいが、同じ層として扱ってはならない。

必要な操作面は、小さく名前付きの public contract として、その意味を所有する層から
公開する。consumer が内部 fixture、renderer state、source text を再解釈して不足した
contract を補ってはならない。必要な contract がなければ、所有者を拡張し、不在を
明示的に失敗させる。

便利な挙動を置くときは、それを判断できる最小の所有者を選ぶ。別の component、scene、
adapter、ゲームでも同じ意味にならないなら、generic な構文や runtime のデフォルトに
してはならない。

## 文法の一貫性と合成性

`.puzzle` の文法は、ユーザーが一度覚えた概念を別の場所でも再利用できるようにする。
同じ意味は可能な限り同じ形にし、同じ見た目に異なる意味を持たせない。ただし一貫性は
機械的な全域許可ではなく、所有者と実行意味の一貫性である。

入れ子はまず owner-scoped な木構造を表す。各 owner は受け入れる子、定義、葉文法を
決める。generic parser や consumer が見た目から葉の種類や実行意味を推定してはならない。
具体的な block、definition、binding、inline 記法は `AUTHORING_SYNTAX.md` が所有し、
parser/editor の構造契約は `SOURCE_ANALYSIS_CONTRACT.md` が所有する。

definition の値種別も owner schema が決める。schema が列挙する閉じた選択肢は bare
atom、作者が宣言・参照する名前は identifier、自由な文字列は quoted string とする。
bare atom の意味は key と owner scope によって決まり、同じ綴りを言語全体の予約語には
しない。parser、補完、highlight、diagnostic、lowering は同じ schema metadata を読み、
consumer が source text から値種別や enum domain を再推定してはならない。

新しい機能は、既存の構文パターン、所有者境界、lowering 規則、component contract、
lifecycle の一例として説明できるべきである。特定の名前や adapter だけが一般概念を
迂回する分岐、似た機能ごとに増える小さな専用言語、表示結果から作者が書いていない
連動を推定する挙動は避ける。

文法の DRY は parser code の字面を揃えることではない。同じ意味の構造を、同じ概念、
同じ所有者、同じ lowering 経路として扱うことである。

## ドキュメントの読者

ユーザー向け文書は、作者が `.puzzle` を読み書きし、実行結果を理解して修正できることを
目的とする。canonical な書き方、短い例、観察できる挙動を優先し、内部 IR、実装都合、
未確定の設計論を最初の理解に混ぜない。

開発者向け文書は、構文、lowering、runtime、adapter の所有者境界を壊さず変更できる
ことを目的とする。実装手順と局所契約は対象フォルダの `AGENTS.md` に置き、ユーザー向け
説明や最上位原則へ複製しない。

## コミュニケーションとしての例

例は、ユーザーとのコミュニケーションにおいて意図を固定する道具である。最小盤面、
入力、期待結果、反例によって、自然言語だけでは曖昧な境界を確認する。

例は後にテストへ変換されてもよいが、最初の役割は「何を意味しているか」を人間と AI
の間で揃えることである。意図が曖昧なときは、抽象的な機構を増やすより先に例を求める
か、比較できる例の候補を生成する。

## ルールシステムの方向性

ルールは、離散的なゲーム状態に対する局所的で検査可能な変化を表現する。

重視するもの:

- 名前付きルール、オブジェクト、プロパティ、条件、フェーズ
- テストとしての例
- トレース可能な実行
- AI 編集のための安定した識別子
- 決定論的な競合解決

避けるもの:

- ルール内の任意コード
- 暗黙の副作用や見えない依存関係
- 無制限に肥大化するグローバル状態
- トレースで説明できない挙動
- パズル推論を改善せず、一般性だけを増やす機能

追加の表現力は、局所ルールの核を不透明にするのではなく、名前付き query、condition、
macro、phase、event、validation、trace annotation などの明示的な拡張点として外側に足す。

## ゲーム固有性の優先

このシステムは、作りたいゲーム群の性質に合わせて偏ってよい。汎用性は自動的に価値
ではない。この形式のパズルゲームで明瞭さ、表現力、反復速度、検証可能性を高めるなら、
狭い機能を優先する。

## 制作環境の方向性

制作環境は軽量であるべきである。まず必要なのは、意図、ルール、例、レベルを編集し、
実行結果を検査し、AI の支援で修正し、回帰テストを残せる短いワークフローである。

Web と Desktop は編集、プレビュー、コンパイルの中核を共有し、ホスト環境に依存する
ファイルアクセスと権限だけを adapter 境界で分ける。具体的な運用契約は
`crates/html_editor/AGENTS.md` と `src-tauri/AGENTS.md` が所有する。

## 設計境界

このプロジェクトは意図的に特殊化されている。プラットフォーマー、アクションゲーム、
物理シミュレーション、リアルタイムゲーム、ノベルエンジン、任意のインタラクティブ
メディアを作る汎用エンジンにはしない。

新機能は、可視性、操作性、普遍性、人間・AI・エンジンのフィードバックループを
改善するかで評価する。表現力は増えるが、ルールの挙動が見えにくく、テストしにくく、
説明しにくくなる機能は、拒否するかコアモデルの外に置く。
