# Claude Agent Notes

`AGENTS.md` is the authoritative repository-wide instruction source. Before
changing this repository, read it and follow its routing to the owner-specific
`AGENTS.md` files and additional references relevant to the task.

This file routes Claude-compatible agents to the shared instructions; it does
not define a parallel repository policy.
