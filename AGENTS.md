# Agent Notes

This folder contains documentation outputs and related documentation material.

Generated web exports such as `index.html` must not be patched directly. Update
the source documentation or generator input, then regenerate only when that is
the explicit task.
