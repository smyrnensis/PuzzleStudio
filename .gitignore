/target
/tools/puzzle_feedback/target
node_modules/
crates/html_editor/web/node_modules
.DS_Store
archive/
AI generated game/

# Local tool and editor state.
.claude/settings.local.json

# Local environment files may contain host paths or secrets.
.env
.env.*
!.env.example
!.env.*.example

# Temporary files from editors and failed merges.
*.tmp
*.bak
*.orig
*~

# External PS Next checkout used for PS 3D extraction work.
# Keep notes/patches/examples in this repo, but do not vendor the fork body.
PS_EXTRACTION/upstream/PuzzleScriptNext/
