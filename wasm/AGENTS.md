# Agent Notes

This folder contains generated WebAssembly artifacts.

Do not patch generated `.wasm` binaries directly. Update the owning Rust/source
crate and regenerate through the normal build/export path only when explicitly
intended.
