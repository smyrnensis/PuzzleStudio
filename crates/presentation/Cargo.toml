[package]
name = "puzzle-presentation"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
puzzle-assets = { path = "../assets" }
puzzle-runtime-contract = { path = "../runtime_contract" }

[dev-dependencies]
image = { version = "0.25.10", default-features = false, features = ["png"] }
serde_json = "1"
