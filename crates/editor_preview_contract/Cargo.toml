[package]
name = "puzzle-editor-preview-contract"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
puzzle-authoring = { path = "../authoring" }
puzzle-runtime-contract = { path = "../runtime_contract" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
