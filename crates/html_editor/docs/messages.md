# Messages
Use `message` to show text before or after a level map.

```puzzle
levels {
  level intro {
    message "Push the box."
    PBG
    message "Room clear."
  }
}
```

Messages before the map run when the level starts.

Messages after the map run when the level clears.

