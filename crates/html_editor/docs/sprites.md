# Sprites
Use `sprites` to define how objects look.

```puzzle
puzzle sokoban {
sprites {
Box {
#b5651d #f4a261
00000
01110
01110
01110
00000
}
}
}
```

Colors come first. The following rows draw the sprite.

Use `layers` for collision and `legend` for level characters.

