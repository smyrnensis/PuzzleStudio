# PuzzleStudio starter

This managed starter project is the default workspace for the GitHub Pages editor.

Edit `game.puzzle`, preview it, then export or download your own project. The
starter is embedded in the Pages release; it does not expose or load the
repository's local `games/` directory.
