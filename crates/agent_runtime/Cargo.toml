[package]
name = "puzzle-agent-runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
puzzle-core = { path = "../core" }
puzzle-lang = { path = "../lang" }
puzzle-play = { path = "../play" }
puzzle-solver = { path = "../solver" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
