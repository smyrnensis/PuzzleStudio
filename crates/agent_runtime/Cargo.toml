[package]
name = "puzzle-agent-runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
puzzle-solver-runtime = { path = "../solver_runtime" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
