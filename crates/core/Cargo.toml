[package]
name = "puzzle-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
name = "puzzle_core"
path = "src/lib.rs"

[dependencies]
puzzle-kernel = { path = "../kernel" }
