[package]
name = "puzzle-wasm-game"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
name = "puzzle_wasm_game"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
puzzle-wasm-player = { path = "../wasm_player", features = ["source-runtime"] }
