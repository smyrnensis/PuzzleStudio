[package]
name = "puzzle-grid3d"
version = "0.1.0"
edition = "2024"
license = "UNLICENSED"

[dependencies]
puzzle-core = { path = "../core" }
puzzle-kernel = { path = "../kernel" }
serde = { version = "1", features = ["derive"] }
